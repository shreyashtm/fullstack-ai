No file scaffold provided on purpose — **Independent** guidance level (see
`project/milestones/milestone-07-bigquery.md`). Needs your own real GCP
project with BigQuery enabled — this milestone can't run entirely
locally. Build your export script, `schema/`, and `queries/` however you
want; the two empty folders here are just the suggested split, not a
requirement.
