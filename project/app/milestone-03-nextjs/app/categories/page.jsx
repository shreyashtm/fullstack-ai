// Concept: next.data-fetching — Server Component, fetches + renders, no interactivity needed

export default async function CategoriesPage() {
  // TODO: fetch("/api/expenses"), aggregate totals by category, render
  return <p>TODO: category totals</p>;
}
