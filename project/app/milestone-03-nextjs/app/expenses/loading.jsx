// Concept: next.loading-error-states — shown automatically while the
// route segment's Server Component is fetching (Suspense under the hood).
export default function Loading() {
  return <p>Loading expenses…</p>;
}
