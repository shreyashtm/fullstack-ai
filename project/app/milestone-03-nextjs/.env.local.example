# Server-only — never prefixed with NEXT_PUBLIC_, never readable from a Client Component.
EXPENSE_API_SECRET=replace-me

# Client-visible — bundled into the browser JS. Only put non-sensitive values here.
NEXT_PUBLIC_SYNC_ENABLED=true
