"use client"; // the only piece of the add-expense flow that needs interactivity

export function ExpenseForm() {
  // TODO: controlled inputs, POST to /api/expenses on submit
  return <form>{/* TODO */}</form>;
}
